# reconcile-engine
A deterministic C++20 batch engine for reconciling conflicting inventory records across disparate systems. Enforces strict schema validation, verifies checksums, and writes an append-only audit log with full deterministic replay — built for correctness-critical, auditable logistics environments.
